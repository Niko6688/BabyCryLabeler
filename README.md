# 👶 婴儿哭声精准全自动标注系统 (BabyCryLabeler) 详细使用指南

本系统是一套专为婴儿哭声数据集制作量身打造的**高精准度自动/半自动音频标注平台**。系统集成了 **Vite-Express Web 后端服务**、**Electron 跨平台桌面客户端** 以及 **基于 Python OCR 的投屏图像时间线二次校验脚本**，构建了端到端无人值守的智能化数据标注流水线。

---

## 🧭 目录索引

1. [一、 开发者调试与本地运行指南](#一-开发者调试与本地运行指南)
2. [二、 桌面端程序打包与分发指引](#二-桌面端程序打包与分发指引)
3. [三、 手机与电脑端物理连接指南 (Scrcpy 投屏)](#三-手机与电脑端物理连接指南-scrcpy-投屏)
4. [四、 多模态标注内容智能匹配与识别逻辑](#四-多模态标注内容智能匹配与识别逻辑)
5. [五、 Python OCR 辅助标注工作流配置](#五-python-ocr-辅助标注工作流配置)
6. [六、 网页版 (Web) 大容量音频导入攻略](#六-网页版-web-大容量音频导入攻略)
7. [七、 一键深度安全卸载与残留清理指南](#七-一键深度安全卸载与残留清理指南)
8. [八、 高频热点常见问题 F.A.Q. & 故障排查 (注意事项)](#八-高频热点常见问题-faq--故障排查-注意事项)
9. [📈 历史迭代与 Bug 修复记录 (Changelog)](./CHANGELOG.md)

---

## 一、 开发者调试与本地运行指南

本系统支持 Windows、macOS 以及 Linux 跨平台运行。首次在本地部署或二次开发，请按以下步骤操作：

### 1. 终端/命令行工具的调用方式
* **Windows 系统**：按下键盘上的 `Win + R` 键，在运行窗口中输入 `cmd` 后点确定，即可调出黑色的 “命令提示符” 窗口。
* **macOS 系统**：按下 `Command (⌘) + 空格` 调出 Spotlight 搜索，输入 `终端` 或 `Terminal` 并回车。

### 2. 定位到项目文件夹 (使用 `cd` 指令)
由于程序需要进入项目对应的物理目录执行构建，需要确保终端的工作路径正确：
* **Windows 路径定位**（假设源码保存在 D 盘的 `BabyCryLabeler` 主文件夹中）：
  ```cmd
  D:
  cd D:\BabyCryLabeler
  ```
* **macOS 路径定位**（最爽捷方式：先在终端中输入下述命令前半段加一个空格，再从访达内**直接拖拽**该项目文件夹到终端窗口中，路径将自动生成）：
  ```bash
  cd /Users/您的用户名/Desktop/BabyCryLabeler
  ```

### 3. 一键安装依赖并运行
在进入对应的项目文件夹路径后，输入并执行以下脚本指令：
* **第一步：同步拉取与安装项目所需的全部底层 npm 包模块**：
  ```bash
  npm install
  ```
* **第二步：启动 Express 后端网关与前端 Vite 的热重载本地服务**：
  ```bash
  npm run dev
  ```
> [!IMPORTANT]
> 成功启动后，终端将输出对应的物理访问链接，您可在浏览器（推荐使用最新版 Google Chrome 获得最流畅、原生的硬件渲染服务）中直接访问：👉 **`http://localhost:3000`**

---

## 二、 桌面端程序打包与分发指引

为方便其他不具备 Node.js/Vite 运行环境的纯业务标注人员，您可以将本系统打包成开箱即用的**独立安装程序** (`.exe` / `.dmg`)。

### 1. 确保安装全部构建必备期依赖
在执行任何桌面端运行、打包或编译命令前，您**必须**首先确保本地已完整安装了项目的所有依赖包（含 `electron`、`electron-builder` 等核心开发/打包工具链）：
```bash
# 这会同步拉取 package.json 中配置的所有 dependencies 与 devDependencies 生产开发双驱套件
npm install
```

### 2. 极简开发运行测试
如果您想以原生桌面窗口模式（Electron Developer Shell）在本地调试运行程序：
```bash
npm run desktop:dev
```

### 3. 独立客户端一键编译构建
1. **全英文物理路径**：请确保该项目解压和打包编译机所在的全部父文件夹路径中**不含中文字符或特殊空格**，避免因为部分平台下底层编译链接器（NSIS/wine）无法解析非 ASCII 编码而报错崩溃。
2. **执行打包指令**：
   * **Windows 编译机**（生成一键安装 `.exe` 格式）：
     ```bash
     npm run desktop:build:win
     ```
   * **macOS 编译机**（生成苹果专属 `.dmg` 安装包）：
     ```bash
     npm run desktop:build:mac
     ```
打包完成后，所有的独立可执行安装文件都将输出在根目录下的 **`dist-desktop/`** 文件夹中。

---

## 三、 手机与电脑端物理连接指南 (Scrcpy 投屏)

在使用自动/半自动标注流程前，需要将安装了婴儿监护 App 的物理手机投屏到运行本系统的电脑桌面。推荐使用轻量、免费且免安装的开源投屏神器 **`scrcpy`**。

### 步骤 1：开启安卓手机的 “USB 调试” 开发者选项
1. 打开您的安卓手机，依次点击 **设置 ➔ 关于手机**。
2. 连续快速点击 **“版本号 (Build Number)”** 7-10 次，直到屏幕底部提示 *“您现在处于开发者模式”*。
3. 返回主设置菜单，进入 **系统/附加设置 ➔ 开发者选项**。
4. 找到并打开 **“USB 调试 (USB Debugging)”** 开关。

### 步骤 2：建立物理连接与投屏
1. 用靠谱的高速 USB 数据线将手机物理接入电脑。
2. 手机亮屏并弹窗 *“是否允许在此设备上允许 USB 调试”*，勾选 **始终允许** 并点击确定。
3. 在电脑的终端中运行：`scrcpy`。电脑桌面将立刻弹出手机镜像窗口。

### 步骤 3：高保真音频环境路径推荐
* **扬声器方案（最省力操作）**：将电脑扬声器与麦克风置于同一个安静空间中，调大扬声器物理音量，促使手机能完全将高保真声波吸入分析。
* **物理内录反馈方案（深度推荐！深夜无人值守无声高品质专用）**：用最便宜的 3.5mm Aux 对录音频跳线将电脑的耳机物理输出口直接硬插接到安卓手机的音频接收孔上。这样在播放哭声时，电脑将完全无杂音，且没有外界的咳嗽声、风扇噪、或者家属说话声干扰，保证标注准确率达 99.9%！

---

## 四、 多模态标注内容智能匹配与识别逻辑

当手机上的婴儿监护 App 在听到哭声判定后，屏幕上对应的报警历史记录会生成如 `宝宝哭了 (饥饿)`、`宝宝哭了 (需要拍嗝)` 等指示牌。系统内置了四种截然不同的多模态匹配与识别逻辑：

| 标注模式 | 操作交互类型 | 键盘热键 / 适配接口 | 适用实操场景 |
| :--- | :--- | :--- | :--- |
| **手动精细打标 (Manual)** | 人工 100% 盲听审听打标 | 主物理键 `1`、`2`、`3`、`4`、`5`（详见键盘区说明） | 适合复杂长尾样本深度鉴别与细微切片核对 |
| **智能直连模式 (Direct)** | 🌟 **(最力推荐)** 自动双向同步 | 回环局域 API `http://127.0.0.1:3124/sync` | **全软件免聚焦/免剪贴板 100% 稳定运行**。在沙盒或多标签页/后台场景下依然飞速、安全、自动流转标注。 |
| **高频剪贴板监听 (Clipboard)** | 半自动轮询拦截（1.5s 触发） | 在投屏端双击文字复制或借助 OCR 工具将诊断结果推入粘贴板 | 适合对定制录音、特殊投屏或模拟器进行快速手动划词/备份粘贴 |
| **物理文件监测器 (File)** | 全自动无人值守数据吞吐 | 读取并在物理后台解析 `./demo_audios/result.txt` | 适合配合经典 OCR 脚本定时写入机制（支持备份与自定义扩展） |

### ⌨️ 高频无物理鼠标干预键盘打标流（手动模式专属）
在进行人工精细化标注音频队列时，无需低头寻找鼠标，系统深度映射了标准键盘流：
* **主按分类键**：按下 **`1`**（饥饿）、**`2`**（不舒服）、**`3`**（犯困）、**`4`**（需要拍嗝）、**`5`**（烦躁/情绪）。按下即可秒级入库并**瞬时加载下一首**。
* **全局播放控制键**：按下 **`空格 (Space)`** 键。可在任何时候对当前播放中的音轨进行优雅的播放与暂停操作，辅助判听。

---

## 五、 Python OCR 辅助标注工作流配置

项目配套的 Python 自动化脚本 `baby_cry_ocr_helper.py` 是全自动流水线的“眼睛”和“双向直连总调度”。

### 1. 时间轴防错标精准闭环算法原理
传统的直接匹配会导致一个严重后果：由于图灵 App 本身神经网络推断有时滞（可能推延 3~10 秒），在切歌时，盲目截图可能会把几分钟前甚至是上一个录音产生的历史记录（如残留的“饥饿”）误打标给当前才播放了 2 秒钟的新音频。

**Python 助手通过时间比对解决了这个问题**：
1. 前端每开始播放、结束播放当前哭声音频，都会自动向本地微型后台同步高精度状态。
2. Python OCR 截屏解析时，不仅会读取诊断文本（例如：`饥饿`），还会利用文字框周围的剪切布局，提取出这条历史报警所带有的小时/分钟。
3. 判定该警报的确切发生时机是否吻合在 **本首音频开播后的合理容忍物理窗口** 之中。
4. 如果数据异常、超时未出现新警报、或读取到的是陈旧废数据，一律向系统发起 **`skip` (安全跳过标注)** 指令。杜绝噪声。

### 2. 精准极速配置环境 (一行接一行)
1. 在项目所在的目录下打开命令行。
2. 运行包安装命令：
   ```bash
   pip install requests pillow opencv-python numpy pyperclip easyocr
   ```
   > [!TIP]
   > 如果您身处国内，网络下载由于外部带宽因素受到阻碍，请追加清华官方源镜像快速通畅安装：
   ```bash
   pip install requests pillow opencv-python numpy pyperclip easyocr -i https://pypi.tuna.tsinghua.edu.cn/simple
   ```
3. **环境故障救治**：若终端冷冰冰提示 `'pip' is not recognized as an internal or external command`，则是未添加 PATH 软链接，请重新运行 Python 安装包，并确保勾选底部的 **`Add Python to PATH`** 复选框。

### 3. 🥇 极速启用「2. 助手直连模式」（全自动、免聚焦、免剪贴板，强力推荐！）

> [!IMPORTANT]
> **关于浏览器安全沙盒限制：**
> 浏览器对读取剪贴板极其严苛。当系统窗口失去焦点、被拖入后台、或者处于 iframe 预览区域中时，浏览器默认会系统性**拒绝剪贴板读取请求**。
> 为此，我们全新升级并**独家支持“本地环回智能直连模式”**！不流经外网，完美避开浏览器一切安全限制：

1. **摆放手机投屏窗口**：
   * 将您的手机通过 `scrcpy` 投屏在电脑屏幕上。
   * 确保您的手机监护 App 处于历史报警界面。
2. **在主界面选中「2. 助手直连模式」**：
   * 打开网页后，在控制台上将会看见 **「2. 助手直连模式」** 已默认激活，下方将显示 *“正在等待连接...”*。
3. **一键开启后台 Python OCR 眼睛脚本**：
   * 在您的终端/命令行中，一键启动 Python 调度引擎。它启动时会**全自动在本地启动一个高速、安全的轻量微服务（Port 3124）**：
     ```bash
     python baby_cry_ocr_helper.py
     ```
   * 脚本启动成功后，将输出：`🚀 [网关极速直连就绪] 本地微型后台 API 同步服务已激活`。
   * 同时，网页端状态会瞬时从等待变为 **`● 助手已连接`**（绿字高亮），进入尊贵的直连巡航快车道！
4. **尽情享受全无人值守打标**：
   * 只要与脚本建立直连，在音频进入间隔等待期时，Python 脚本会自动高速捕获屏幕，并把分析出的标签毫秒级直接接口同步给网页，完成保存与切歌，即使网页在后台也能安全、欢畅地通宵工作！

### 4. 📂 启用备用「4. 文件监听模式」或「3. 剪贴板模式」
如果您更习惯传统的基于文件的进程协同或使用剪贴板来中转备份，本系统也提供了完美的兼容保障：
1. 在网页端点击激活 **「4. 文件监听模式」**，状态会变更为：`📂 正在监听 result.txt...`。
2. 运行 `python baby_cry_ocr_helper.py` 时，如果不小心断开了直连，系统会全自动将判定到的分类词同步写入 `result.txt` 硬盘文件。
3. 网页的文件监听底层会通过热重载管道，在一瞬间读取该结果、保存当前状态并极速开启下一首，免去一切死板等待！

---

## 六、 网页版 (Web) 大容量音频导入攻略

由于浏览器内部的“黑盒沙盒安全属性 (App Sandbox Security)”，任何一个标准的网页组件，在非宿主物理环境 (Electron) 下，均**无法绕过用户操作直接扫描你硬盘里的任意文件夹路径**。

为了保障零部署门槛，本系统提供了以下三种高品质解决姿势：

```
+--------------------------------------------------------+
| 方案A (演示用)     ➔ [ 自动合成本地模拟 Demo 音频 ]     |
| 方案B (极力推荐)   ➔ [ 拖入或点击一次性框选数百条音频 ]  |
| 方案C (局域网级)   ➔ [ 点击"扫描指定目录"并输入物理盘符 ] |
+--------------------------------------------------------+
```

* **方案 A：本地合成模拟仿真包（测试体验）**
  * 点击系统上的 “自动合成本地模拟演示音频 (Demo)” 标签，后端直接从服务器物理层面的 `./demo_audios/` 文件夹下为您吐出 5 首具有声源特征的高仿真声波，完美打通测试。
* **方案 B：拖拽与高维批量选择（极力推荐，最常用）**
  * 在 Web 主界面，直接一秒拖拽一个包含有数百条、数千条哭声多格式（支持 `.mp3`、`.wav`、`.m4a`、`.ogg`、`.flac`、`.aac` 等）的外部 Finder 窗口或 Windows 资源管理器文件夹到页面中心的拖拽区域。
  * 前端将结合标准的 `URL.createObjectURL` 技术为您快速生成虚拟解混流，保障浏览器沙盒内直接高效、极速不丢包持久化运行。
* **方案 C：输入完整绝对路径（强强对接方案）**
  * **在与服务器同台计算机运行时**，点击主控配置面板的 **“扫描指定路径”** 按钮。
  * 输入物理硬盘下的完整本地绝对地址（如 Windows：`D:\datasets\infant_cries`，或 Mac：`/Users/username/audio-dataset`）。网页服务端 Express 将以底层 Node 超神文件级别读取，并在 10 毫秒内完成数据索引注入！

---

## 七、 一键深度安全卸载与残留清理指南

本系统提供了一套科学、安全且深度底层的清理体系，确保在您完成数据集制作，不再需要本系统时，能以**“零温床残余”**的完美状态将整个物理应用从硬盘中蒸发，不给操作系统平添垃圾。

### 1. 自动彻底覆盖的物理清除扇区
* 🗑️ **应用程序（App Files）**：清除存放的所有物理二进制可执行客户端（macOS 端的 `.app`、Windows 端的 `unpacked` 绿色程序等）。
* 📂 **系统高速缓存与运行日志**（Electron、Vite 运行期间会在系统全局路径下创建各种配置快照）：
  * Windows：`%APPDATA%/BabyCryLabeler`（即用户数据应用隔离核心）
  * macOS：`~/Library/Application Support/BabyCryLabeler`
* 🗃️ **核心标注成果数据**：即输出的原始 `labeled_output.csv`、`labeled_output.json` 标注进度件。**（注意：删除此项时，卸载系统会发出友好安全确认，极力为您自主心血保驾护航！）**

### 2. 跨平台一键干净无痕清理执行步骤
* **Windows 系统用户（自动提权与无感回退体系）**：
  * 在项目的主物理根路径下直接用鼠标 **双击** 👉 **`Uninstall_Windows.bat`** 运行。
  * 该卸载工具将智能检测打包运行环境。如果用户没有配置 Node 软链接环境，批处理会柔性降级到极其纯净的原生 CMD 指令序列中，对注册表及 `%APPDATA%` 相关应用支持残存彻底执行粉碎式擦除，提供通俗易懂的中文反馈。
* **macOS 系统用户（浮动终端强制执行清理体系）**：
  * 在访达主项目根物理路径下直接 **双击** 运行 👉 **`Uninstall_macOS.command`**。
  * 该卸载工具将拉起交互式的 Terminal 控制框，对挂载的 `/Applications/BabyCryLabeler.app` 等核心组件与系统资源库进行全面肃清。
* **npm 指令终端极速清理**：
  * 如果您是使用终端进行开发的工程师，直接在项目根路径运行一行命令即可：
  ```bash
  npm run uninstall
  ```

---

## 八、 高频热点常见问题 F.A.Q. & 故障排查 (注意事项)

<details>
<summary><b>Q1：系统会因为我中途关闭应用，导致之前成百上千首已经辛辛苦苦标注的文件全部丢失吗？</b></summary>
<br>

**答：绝对不会！系统设计了金融级双持久重写防丢引擎：**
1. **客户端毫秒极速暂存 (LocalStorage Guard)**：
   标注操作只要成功的瞬间，前端会一毫秒内同步将其作为最纯粹的 Key-Value 备份于您当前浏览器的物理存储树。即便遭遇断电、死机、系统强退，重新一键载入后，界面数据会完整地在原地还原。
2. **服务端物理多盘直接落盘 (Hard-Write System)**：
   在启用开发调试服务或使用 Electron 桌面应用连接运行时，所有的标注标签和播放切片索引会同步写入在当前项目根目录的最核心硬盘源物理文件：👉 **`progress.json`** 和 **`labeled_output.csv`** 中。
3. **备份导出恢复**：
   在主界面，您可以在任何时候点击 **“导出/下载标注历史 JSON”**，这会产生一个极其轻便的数据集快照件。在换电脑或重装应用时，只需要在设置里点击其“导入 Progress 历史 JSON”，就能秒级恢复所有的队列状态，安全感拉满！
</details>

<details>
<summary><b>Q2：运行：<code>npm run desktop:build:mac</code>，报错：<code>Missing script: "desktop:build:mac"</code> 是为什么？</b></summary>
<br>

* **原因分析**：
  这代表您在个人电脑上正在使用的是**较老版本**的源码文件。较早版本的 `package.json` 中的构建脚本中，可能还没有我们在后续高度适配版中补充添加的 macOS 构建专用运行热键指令。
* **一分钟一键修复方式（两个方案，任选其一即可）**：
  * **方案 A（无需下载重新配置）**：
    用任何您熟悉的文本编辑器（如 VS Code, Windows 记事本等）打开项目根目录下的 **`package.json`**。在 `"scripts"` 内，手动插入、补上以下这几行声明：
    ```json
    "desktop:build:win": "npm run build && electron-builder --win nsis",
    "desktop:build:mac": "npm run build && electron-builder --mac dmg"
    ```
    补齐后再次保存，即可完美在终端中再次平稳运行 `npm run desktop:build:mac`。
  * **方案 B**：
    您可以在 AI Studio build 的操作台页面最右上角的 **“Settings (设置)”** 内选择 **“Export to ZIP”** 重新导出、下载一份最新的系统包解压到本地运行一次 `npm install`。
</details>

<details>
<summary><b>Q3：为什么由于图挂导致终端里报了一长串含有 <code>wine64: bad CPU type in executable</code> 这样的故障报错？</b></summary>
<br>

* **根本病因**：
  * 默认打包命令会试图将程序打包编译出 Windows 支持的 `.exe` 安装程序。
  * **然而，如果您在苹果 Mac 系统下运行该命令**（特别是 Apple Silicon M1/M2/M3 等 ARM64 处理器电脑上），要把代码模拟编译为针对微软系统的 `.exe` 二进制文件，打包底层自动运行的 `electron-builder` 会调起专供跨平台执行的 `Wine64` 虚拟机软件和 `rcedit` 签名工具。随着苹果 macOS 彻底清退 32 位底层、以及对未经 Rosetta 编译授权的 ARM64 进行防跨界内核锁定，导致抛出 CPU 坏类型报错。
* **完美无错解决大招**：
  * **在 Mac 主力机上**：请严格运行该特定命令，生成极速、纯净适配 macOS 的标准 `.dmg` 安装客户端：
    ```bash
    npm run desktop:build:mac
    ```
  * **在 Windows 主力机上**：请严格运行以下命令，完美获得 Windows `.exe` 安装客户端包：
    ```cmd
    npm run desktop:build:win
    ```
</details>

<details>
<summary><b>Q4：在 macOS 下双击运行一键安全卸载 <code>Uninstall_macOS.command</code> 脚本时，系统突然弹窗粗暴拦截：“无法打开，因为它来自身份不明的开发者”？</b></summary>
<br>

* **原因分析**（macOS 著名的 Gatekeeper 门禁隔离区安全策略）：
  当该命令文件是通过网络（或直接通过浏览器打包下载）来到您电脑的“下载”文件夹中后，苹果系统在它落地的首个微妙瞬间，会对该文件物理属性深度打上一个 “网络引入（com.apple.quarantine）” 的隔离标签。
* **极速突破苹果拦截并正常运行的动作（推荐方法A最为方便舒适）：**
  * **方法 A：右键强制授信并强行开启**
    1. 在访达中找到 **`Uninstall_macOS.command`**，**不要直接鼠标双击！**
    2. 使用鼠标 **右键点击** 它（或同时长按键盘上的 `Control` 键并用手单击该文件）。
    3. 在新出现的上下文菜单中点击选择第一行的 **“打开 (Open)”**。
    4. 此时电脑会弹出一个特殊的验证对话框，其上赫然暴露出深蓝色的 **“依旧打开”** 或 **“打开”** 选项。点击它即可运行。
  * **方法 B：在系统偏好中为其物理放行**
    1. 正常双击它并收到拦截警告弹框后，点击“好”关闭当前的浮窗。
    2. 打开左上角的苹果菜单，进入 **“系统设置” ➔ “隐私与安全性”**。
    3. 您会看见系统的提示小文字：“*已阻止打开“Uninstall_macOS.command”，因为它来自身份不明的开发者。*”，点击 **“仍要打开 (Open Anyway)”** 并完成指纹/锁屏密码验证即可。
</details>

<details>
<summary><b>Q5：运行一键安全深度卸载脚本时，为什么终端会闪红字报错：<code>ENOTEMPTY: directory not empty</code>（或者在 Windows 上显示权限不足或 <code>EBUSY</code> 拒绝访问）？</b></summary>
<br>

* **原因分析（文件或物理进程产生内核“独占占用锁”）**：
  1. 当遇到卸载动作操作在部分平台报错，一定是电脑上**标注客户端或者后台 Express 服务依旧在高速运转**。软件主窗口、任务栏小托盘、或是底层 Node 子进程仍在不断监控物理文件夹下的进度文件（`progress.json`）并形成持久句柄。
  2. 极少数状况。在 Windows 操作系统中运行、操作 CMD 的命令行或者当前登陆的用户**无系统最高超级管理员**（Admin）权限。
* **终极解决方法**：
  1. **完全退出软件**：请确保已经完美并关闭了 `BabyCryLabeler` 客户端的主程序窗口，且在任务管理器中杀掉任何包含有 `BabyCryLabeler` / `node` 的残留进程。
  2. 运行终极一键指令：
     ```bash
     npm run uninstall
     ```
</details>

<details>
<summary><b>Q6：在我 Mac 电脑的桌面上突然出现了一个像银色“外置移动硬盘（或者磁盘）”一样的古怪图标，名字写着 <code>BabyCryLabeler-1.0.0-arm64</code>，它怎么移不进废纸篓，右键删不掉？这是怎么回事？</b></summary>
<br>

* **深度释疑**：
  * 您在 Mac 桌面上忽然变出来的那个“银色磁盘”名为 **“挂载的磁盘分卷/虚拟磁盘驱动（Mounted Volume）”**。
  * 这是苹果挂载 `.dmg` 镜像以安装应用程序的正常逻辑，并不是木马或文件。它无法像普通文件一样移入废纸篓。
* **彻底踢走它的简单操作方案（任选其一）：**
  * **方法 A：经典物理推出（拖拽流）**：按住这个银色盘符图标不放，将其拖拽到 Dock 栏最下方的 **“废纸篓”**（此时废纸篓会变成 **推出符号 `⏏`**），松开鼠标即可。
  * **方法 B：右键推出**：右键点击它，选择菜单中的 **推出 "BabyCryLabeler" (Eject)**。
  * **方法 C：快捷键**：单击选中该盘符，按下组合键 **`Command (⌘) + E`** 瞬间弹出。
</details>

<details>
<summary><b>Q7：我已经在应用程序（Applications）里面彻底删除了该软件，但是每次在 Mac 上打开 Launchpad（启动台）的时候底下的图标里还依然粘连着这个无法开启的“幽灵残影图标”，怎么消除它？</b></summary>
<br>

* **病因分析**：
  macOS 系统为了保障启动台打开滑动时的流畅度，在系统深层建立了一个 SQLite 数据库缓存区。当你采用非官方方式直接丢弃应用后，数据库未能及时感知，从而残留了一个无法开启的幽灵图标（点上去变问号）。
* **快速、立竿见影强力清除这一残留的方法：**
  * **方法 A：震动点叉流（最快速）**：打开启动台，长按 **`Option (⌥)`** 键。此时图标会疯狂左右摇摆，点击该幽灵图标左上角的 **`X`** 即可。
  * **方法 B：顺水推舟强制自愈流**：在启动台中直接用鼠标点击该幽灵图标，系统会弹出 *“无法找到该应用程序，要将其从启动台移除吗？”*，点击确认即可。
  * **方法 C：100% 彻底一键重建 Launchpad 缓存（终极特效）**：打开终端，输入下方命令并敲击回车：
    ```bash
    defaults write com.apple.dock ResetLaunchPad -bool true; killall Dock
    ```
</details>

<details>
<summary><b>Q8：在 macOS 中双击挂载 <code>.dmg</code> 并运行应用后，由于误选或出于好玩上传了一些本地音频文件，当我为了清空清白点击了“清空/重置”按键并重整旗鼓后，我用鼠标再次在这个位置上传/拖入刚才的这一模一样的一批相同名字音频时，为什么电脑完全卡住、界面没有任何声音和反应了？是不是卡死了？</b></summary>
<br>

* **科学、诚实的底层原理剖析**：
  1. 这是一个**完全正常的浏览器与 Electron 物理事件拦截表现机制**。
  2. 当您上传文件后，React 状态树记录了这批文件的指针。当你点击“清空/重置”后，我们清空了 React 状态，但底层浏览器的 `<input type="file" />` 上的 `.value` 依然指着这批文件。
  3. 当你再次上传一模一样名字的文件时，浏览器认为“被选中内容前后无任何变化”，因而**不触发 `onChange` 事件拦截函数**，导致界面没有感知。
* **完美解决方案（已在最新版中完美解决）**：
  * 我们已经在系统最新升级中，加入了在选取或拖拽完成后，一微秒内强制执行 `TargetInput.value = ''` 清空命令的逻辑。**至此，您再也无须受累于底层机制障碍**，可随心所欲多次重复选取同名文件上传！
</details>

<details>
<summary><b>Q9：在 macOS 中我已经把软件拖拽进了 Applications (应用程序) 文件夹，但是为什么打开 Launchpad (启动台) 里面却找不到它的图标？</b></summary>
<br>

* **原因分析**：
  macOS 的 Launchpad 启动台有其 SQLite 缓存，有时拖入后未能立刻刷新。
* **解决步骤：**
  * **方案 A：手动强制拉入**：在访达中找到 `BabyCryLabeler`，直接用鼠标按住它并拖拽到 Dock 栏上的 Launchpad 图标上松开。
  * **方案 B：强制重构 Launchpad 缓存**：在终端运行：
    ```bash
    defaults write com.apple.dock ResetLaunchPad -bool true; killall Dock
    ```
</details>

<details>
<summary><b>Q10：在 macOS 中双击运行 <code>Uninstall_macOS.command</code> 电脑彻底卸载清理程序，为什么弹窗提示：“无法执行，因为你没有正确的访问权限”？</b></summary>
<br>

* **原因分析**：
  在苹果 macOS 安全架构中，从网络上下载的脚本默认不具有 “可执行权限 (Executable Privilege)”。
* **只需 3 秒钟的完美终极权限重授予解决步骤**：
  1. 打开终端。
  2. 输入 `chmod +x ` （**注意 `+x` 后面必须带有一个空格**）。
  3. 将 `Uninstall_macOS.command` 文件直接用鼠标**按住并拖入**该终端窗口内（自动生成绝对路径）。
  4. 按下 **回车键 (Enter)** 确认授权，即可完美双击双向流转运行！
</details>

---

## 💡 开发声明与持续技术支持

本系统是由 **ni** 独立全栈设计、开发与开源维护。该标注系统目前对广大婴儿健康研发人员、学术界以及物联网生态伙伴 100% 永久免费开放，致力于以高精准度的多模态深度融合方案快速赋能智能语音硬件、母婴看护服务和云端自动化监测。让我们用云端前沿技术注入科技爱心，共同守护每一个可爱的新生儿天使！👼
